#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strcat - appends a string to another string
 * @dest: destination
 * @src: source
 * Return: char*
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
