#include <string.h>
#include "main.h"

/**
 * _strstr - strstr
 * @haystack: parameter
 * @needle: parameter
 * Return: strstr
 */
char *_strstr(char *haystack, char *needle)
{
	return (strstr(haystack, needle));
}
