#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strcpy - copies a string
 * @dest: destination
 * @src: source
 * Return: char
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));

}
